# 更新日记

## 新增插件：随机开启app v1.0.0 (2026-08-31)

### 新增
- **随机开启app**：注入SpringBoard，在设置的随机时间段内选择一个时刻自动打开指定App，**每天最多一次**，第二天自动重置
- 设置 → 随机开启app：启用开关、选择目标App（钉钉/微信/抖音等预设 + 可手动填Bundle ID）、开始/结束时间段、仅限工作日开关
- 适合需要"模拟真人随机使用App、每天一两次"的场景（如钉钉打卡）

## v2.3.1 (2026-08-26)

### 新增 / 优化
- **状态栏整合升级**：「通知关闭 / 正常联网」等状态、**关键词过滤**、**分组** 全部并到同一栏，采用自动换行药丸布局，一行放不下自动换行，彻底告别“被挤到角落/显示不全”
- **可直接点击**：关键词过滤、分组现在是可点按钮——已分组显示组名、点击即可修改分组与关键词
- **按钮与字体加大**：状态/动作药丸、网络按钮、开关标签整体加大，更好点、更大气
- **开关行排版优化**（锁屏/通知中心/横幅/声音/标记 与 仅隐藏角标/隐藏预览/后台断网）：字号调大、间距收紧、标签防截断
- **后台断网说明修正**：该功能断的是 App 的**后台数据流量/后台刷新**，**挡不住系统 APNs 推送**（QQ 退后台仍收到推送属正常）。若想退后台不再收到消息，请用「通知关闭」

> 提示：切后台自动断网、关键词过滤等需要 **重启 SpringBoard（sbreload）** 后完全生效。

## v2.3.0 (2026-08-26)

### 新增功能
- **增强开关**（每个 App 独立）
  - **仅隐藏角标**：彻底隐藏桌面小红点，但通知横幅/锁屏/声音照常，不漏消息
  - **隐藏通知预览（隐私）**：锁屏/横幅只显示 App 名，不显示消息正文（如微信只显示“微信”）
  - **切后台自动断网**：App 退后台即断网，切回前台自动恢复网络，阻止后台偷跑流量/推送
- **关键词过滤**：按 App 编辑关键词黑名单（逗号分隔），通知标题/内容命中即拦截该条，不影响其他推送
- **应用分组**：给 App 打标签分组（社交/游戏/短视频等），可整组一键开启/关闭/断网
- **模式快照**：一键保存当前全部 App 的通知+网络配置，可随时一键回滚
- **拦截日志**：记录被拦截的通知、关键词过滤、后台断网等事件，随时间排查异常推送
- **筛选**：全部 / 已开启 / 已关闭 / 断网，快速过滤要调整的 App
- **反馈**：设置页右上角“反馈”按钮，一键发邮件反馈问题（wacljcr@qq.com）
- **卡片状态标签**：每个 App 直观显示「通知关闭/通知全开/部分开启 / 断网/正常联网 / 增强项 / 分组」

### 修复 / 优化
- 列表卡片行高随功能扩展调整，滑动更顺滑
- 增强开关与网络策略、系统通知设置实时同步

> 提示：切后台自动断网、关键词过滤等需要 **重启 SpringBoard**（sbreload）后完全生效。